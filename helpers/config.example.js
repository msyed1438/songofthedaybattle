//IMPORTANT!
//Create a copy of this file, and rename to config.js
//Then get the spotify client id and secret and replace the empty strings below with them

const secretToken = {
  clientId: "",
  clientSecret: "",
};

export default secretToken;
